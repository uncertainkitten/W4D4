<h2><%= @band.name%> 's jams</h2>

<body>
  <ul>
      <% @band.albums.each do |album| %>
      <a href="<%= album_url(album.id)%>"><%= album.title%></a>
      <%end%>
  </ul>
  <a href="<%= new_band_album_url(@band)%>">New record?</a>

</body>
