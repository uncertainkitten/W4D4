<h2><a href="<%= band_url(@album.band)%>"><%= @album.band.name%></a> 's <%= @album.title%></h2>

<body>
    <form action="<%= album_url(@album)%>" method="post">
      <input type="hidden" name="_method" value="DELETE">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token%>">
      <input type="submit" value="*record scratch*">
    </form>
    <br>
    <a class="links" href="<%= edit_album_url(@album)%>">Needs a remaster?</a>
</body>
